name: (disabled) Legacy FTP Deploy

# Hostinger handles deployment via Git Deployment.
# This workflow is kept disabled to avoid failures due to missing FTP secrets.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy deploy disabled. Use sync-from-sheet + notify-search workflows."